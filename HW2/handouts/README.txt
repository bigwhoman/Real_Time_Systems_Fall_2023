first install python packages in requirements.txt with command bellow : 
python3 -m pip install -r requirements.txt

Then modify the code to change the task tree or run the examples in example_trees 
